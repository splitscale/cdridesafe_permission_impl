name: ridesafe_permission_impl
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.6 <3.0.0'

  # dependencies:
  #   path: ^1.8.0

dependencies:
  permission_handler: ^10.2.0
  ridesafe_core:
    git: https://github.com/splitscale/ridesafe_core.git

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
